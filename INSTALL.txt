REFRESHED BY FAITH — SUPABASE APPLICATIONS SETUP

Problem fixed:
  Could not find the table 'public.applications' in the schema cache

INSTALL:
1. Open the Supabase project used by refreshedbyfaith.com.
2. Open SQL Editor.
3. Create a new query.
4. Open supabase-applications-setup.sql from this ZIP.
5. Copy/run the ENTIRE SQL file.
6. Confirm the final query returns:
       public | applications
7. Return to refreshedbyfaith.com/dashboard and refresh the page.
8. Sign in and test Save Draft.

This creates the applications table used by src/pages/Dashboard.tsx and enables
Row Level Security so authenticated applicants can access only their own records.

IMPORTANT:
This fixes the missing applications table. The Pay $35 button also depends on
/api/create-checkout-session and the payment-provider configuration; those are
separate from this database-table error.
