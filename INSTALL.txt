REFRESHED BY FAITH - DASHBOARD BUILD FIX

Replace the existing file with:
src/pages/Dashboard.tsx

Do not rename the file and do not paste only part of its contents.

This fixes the Vercel errors:
- Dashboard.tsx is not a module
- Cannot find name useEffect
- Cannot find name supabase
- Cannot find name setAuthLoading
- TS18047 supabase is possibly null

After replacing the file, commit/push and let Vercel run:
npm run build
