import {useEffect,useState} from 'react';import {supabase} from '../lib/supabase';
const fields=[['legal_name','Legal name'],['preferred_name','Preferred name'],['phone','Phone'],['date_of_birth','Date of birth'],['current_city','Current city'],['emergency_contact','Emergency contact name'],['emergency_phone','Emergency contact phone'],['sobriety_date','Sobriety date'],['referral_source','Referral source / organization'],['referring_party_name','Referring party name'],['referring_party_relationship','Referring party relationship / role'],['referring_party_phone','Referring party phone'],['referring_party_email','Referring party email'],['employment_income','Employment / income plan'],['recovery_support','Recovery support / meetings'],['medications','Current medications (if any)'],['accommodations','Relevant accommodation needs'],['why_housing','Why are you seeking sober living housing?']];
export default function Dashboard(){const[data,setData]=useState<Record<string,string>>({});const[msg,setMsg]=useState('');const[appId,setAppId]=useState<string|null>(null);useEffect(()=>{(async()=>{if(!supabase)return;const{data:{user}}=await supabase.auth.getUser();if(!user)return;const{data:a}=await supabase.from('applications').select('*').eq('user_id',user.id).order('created_at',{ascending:false}).limit(1).maybeSingle();if(a){setAppId(a.id);setData(a)}})()},[]);async function save(){if(!supabase){setMsg('Supabase is not configured.');return null;}const{data:{user}}=await supabase.auth.getUser();if(!user){setMsg('Please sign in.');return null;}const payload={...data,user_id:user.id,status:'draft'};const r=appId?await supabase.from('applications').update(payload).eq('id',appId).select().single():await supabase.from('applications').insert(payload).select().single();if(r.error){setMsg(r.error.message);return null;}else{setAppId(r.data.id);setMsg('Draft saved.');return r.data.id;}}async function checkout(){const savedId=await save();if(!savedId||!supabase)return;const{data:{session}}=await supabase!.auth.getSession();const res=await fetch('/api/create-checkout-session',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session?.access_token||''}`},body:JSON.stringify({applicationId:savedId})});const j=await res.json();if(j.url)location.href=j.url;else setMsg(j.error||'Unable to start payment.')}return <section className="page dashboard"><span className="eyebrow">APPLICANT PORTAL</span><h1>Your Housing Application</h1><div className="statusbar"><b>Status: Draft</b><span>Complete intake → Pay $35 → Submit → Review → Interview</span></div><div className="formGrid">{fields.map(([k,l])=><label>{l}{['why_housing','recovery_support','medications','accommodations'].includes(k)?<textarea value={data[k]||''} onChange={e=>setData({...data,[k]:e.target.value})}/>:<input type={k.includes('date')?'date':k.includes('email')?'email':k.includes('phone')?'tel':'text'} value={data[k]||''} onChange={e=>setData({...data,[k]:e.target.value})}/>}</label>)}</div><label className="consent"><input type="checkbox" required/> I certify that the information provided is accurate and understand that payment of the application fee does not guarantee admission.</label>{msg&&<p>{msg}</p>}<div className="actions"><button className="btn ghost" onClick={save}>Save Draft</button><button className="btn" onClick={checkout}>Pay $35 & Submit</button></div></section>}
